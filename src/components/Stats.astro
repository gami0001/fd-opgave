<div class="yellow-stats">
  <div class="grid_1-1">
    <div class="text">
      <p class="eyebrow">Experience</p>
      <h2>More than 25 years of experience</h2>
      <p class="subtext">3000+ our clients are insured around the World</p>
    </div>

    <div class="stats">
      <div class="stat-wrapper">
        <div class="stat" style="--percent:85"></div>
        <div class="label">Consistency</div>
      </div>

      <div class="stat-wrapper">
        <div class="stat" style="--percent:95"></div>
        <div class="label">Improvement</div>
      </div>

      <div class="stat-wrapper">
        <div class="stat" style="--percent:90"></div>
        <div class="label">Branching</div>
      </div>
    </div>
  </div>
</div>

<style>
  .yellow-stats {
    background: var(--color-surface-highlight);
    display: grid;
    grid-template-columns: subgrid;
    grid-column: full;
    padding-block: 4rem;
  }

  .yellow-stats > * {
    grid-column: content;
  }

  .grid_1-1 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    > * + * {
      margin-left: 64px;
    }
  }
  .text {
    max-width: 420px;
    p {
      font-size: var(--step--1);
    }
    h2 {
      font-size: var(--step-3);
    }
  }

  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: var(--step--1);
    margin-bottom: 0.5rem;
  }

  .subtext {
    margin-top: 0.5rem;
  }

  .stats {
    display: flex;
    gap: 2.5rem;
  }

  .stat-wrapper {
    display: grid;
    justify-items: center;
    gap: 0.8rem;
  }

  .stat {
    container: stat / inline-size;
    --border-width: 5px;

    aspect-ratio: 1;
    width: 150px;
    border-radius: 50%;
    border: var(--border-width) solid var(--color-surface-primary);

    display: grid;
    position: relative;
  }

  .stat::before,
  .stat::after {
    grid-area: 1 / 1;
  }

  .stat::after {
    counter-reset: stat var(--percent);
    content: counter(stat) "%";
    place-self: center;
    font-size: var(--step-2);
  }

  .stat::before {
    content: "";
    width: 18px;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-neutrals-900);

    offset-path: border-box;
    offset-distance: calc(var(--percent) * 1%);
    offset-anchor: 50% calc(50% - calc(var(--border-width) / 2));
  }

  @media (max-width: 768px) {
    .grid_1-1 {
      grid-template-columns: 1fr;
      > * + * {
        margin-left: 0;
        margin-top: var(--space-s);
      }
    }

    .stats {
      justify-content: center;
      gap: var(--space-s);
    }

    .text {
      text-align: center;
      max-width: 100%;
    }

    .stat {
      width: 120px;
    }
  }
</style>
