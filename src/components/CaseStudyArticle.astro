---
//@ts-nocheck
import DynamicImage from "@components/helpers/DynamicImage.astro";
import caseStudy from "@data/caseStudy.json";
import Button from "./Button.astro";
const { caseName, category, date, image, buttonLabel } = caseStudy.caseDetails;
---

<article class="CaseArticle">
  <header>
    <div class="Dynamic_picture">
      <img src="/images/caseimg.webp" />
    </div>
    <div class="case-details">
      <div>
        <dt>Case Name</dt>
        <dd>{caseName}</dd>
      </div>
      <div>
        <dt>Category</dt>
        <dd class="category">{category}</dd>
      </div>
      <div>
        <dt>Date</dt>
        <dd class="date">{date}</dd>
      </div>
      <Button text="live Preview" variant="yellow" />
    </div>
  </header>
  {
    caseStudy.sections.map((section) => (
      <section class="CaseStudy_text">
        {caseStudy.sections[0] === section ? (
          <h2>{section.title}</h2>
        ) : (
          <h3>{section.title}</h3>
        )}

        {Array.isArray(section.content) ? (
          <>
            {section.content.map((item) => (
              <p>{item}</p>
            ))}
          </>
        ) : (
          <p>{section.content}</p>
        )}
        {section.highlights && (
          <ul>
            {section.highlights.map((highlight) => (
              <li>{highlight}</li>
            ))}
          </ul>
        )}
        {section.image && <img src="/images/storyboard.webp" />}
      </section>
    ))
  }
</article>

<style>
  .CaseArticle {
    display: grid;
    grid-template-columns: subgrid;
    grid-column: full;

    padding-block: 4rem;
  }

  .Dynamic_picture :global(img) {
    border-radius: var(--border-radius-0);
  }

  .CaseArticle > * {
    grid-column: content;
  }

  .case-details {
    background: var(--color-neutrals-900);
    color: var(--color-neutrals-50);

    display: grid;
    grid-template-columns: repeat(4, 1fr);
    align-items: center;

    padding: 54px 60px;
    border-radius: var(--border-radius-01);

    width: min(1000px, 90vw);
    margin: 0 auto;
    dt {
      font-size: var(--step-0);
      font-weight: 600;
    }

    dd {
      font-size: var(--step--1);
    }
  }

  .CaseStudy_text {
    --flow-space: var(--space-m);
    padding: 0 5rem 0 5rem;

    line-height: 1.5em;

    max-width: 950px;
    margin: 0 auto;

    > * + * {
      margin-top: var(--flow-space);
    }

    h2 {
      padding-top: 5rem;
      --flow-space: var(--space-xl);
      font-size: var(--step-3);
      font-weight: 600;
    }

    h3 {
      --flow-space: var(--space-m);
      font-size: var(--step-2);
      font-weight: 600;
    }

    p {
      --flow-space: var(--space-s);
      color: var(--color-neutrals-700);
    }

    ul {
      --flow-space: var(--space-s);
      color: var(--color-neutrals-700);
      margin-bottom: var(--space-xl);
    }

    li {
      margin-bottom: var(--space-xs);
    }
  }

  .CaseStudy_text :global(img) {
    padding-block: var(--space-l);
    margin: 0 auto;
  }
</style>
